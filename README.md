# BREAKOUT

## UBUNTU

```sh

sudo apt-get install doxygen

sudo apt-get install libxss-dev libxxf86vm-dev libxkbfile-dev libxv-dev

sudo apt-get install libxrandr-dev libxinerama-dev xorg-dev libsfml-dev libx11-dev  libglfw3-dev

sudo apt install automake

sudo apt install libtool

sudo apt-get install -y libharfbuzz-dev libpng-dev zlib1g-dev libbz2-dev libbrotli-dev brotli freeglut3-dev freeglut3

sudo apt-get install libudev-dev

sudo apt-get install libopenal-dev libvorbis-dev libvorbisenc2 libvorbis0a libvorbisidec1 libvorbisidec-dev libvorbisenc2 libvorbisfile3 libvorbisidec-dev libvorbisidec-dev vorbis-tools libshout3-dev libtag1-dev libtag1-doc libtagc0 libflac-dev libflac8 libflac++6v5 clang

sudo apt-get install mesa-utils libglfw3-dev libgles2-mesa-dev libosmesa6-dev libosmesa6

```
